type Department {
  id: String!
  name: String!
  history: [LeadershipHistory]
}

type Query {
  departments: [Department]
  departmentsPresidentAndVicePresident: [Department]
}

type Mutation {
  createDepartment(name: String!): Department
}