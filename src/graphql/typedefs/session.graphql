type Session {
  id: String
  session: String
  history: [LeadershipHistory]!
}

type Query {
  sessions: [Session]
  sessionPresidents: [Session]
}

type Mutation {
  createSession(session: String!): Session
}