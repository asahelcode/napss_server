type Session {
  id: String
  session: String
  history: [LeadershipHistory]
}

type Query {
  sessionFacultyAndDeptPresident: [Session]
  sessionFacultyPresidentAndVice: [Session]
  sessionDepartmentPresidentAndVice: [Session]
}

type Mutation {
  createSession(session: String!): Session
}